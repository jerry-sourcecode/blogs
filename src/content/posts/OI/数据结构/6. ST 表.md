---
title: ST表
published: 2026-08-05
updated: 2026-08-05
description: 快速解决 RMQ 问题
tags:
  - OI
  - 数据结构
language: 中文
slug: ds/st
category: 知识点
---

ST 表适用于解决**可重复贡献问题**．

> [!NOTE] 可重复贡献问题
> 在统计时，一个元素被统计多次对结果没有影响（即对于任意的运算 $opt$，存在 $x\ opt\ x = x$），常见的可重复贡献问题有：求区间最值（RMQ）、区间按位与、区间按位或、区间 gcd等．

ST 表使用倍增的思想．我们现在想要快速求序列 $a$ 中的区间最小值，需要维护以下数据：

- `f[j][i]`：从 `i` 开始，长度为 $2^j$ 的序列的最小值．

`f` 数组可以通过递推的方式预处理出来，每次计算 $[i, i+2^j-1]$ 的最小值时，可以拆分为 $[i, i+2^{j-1}-1]$ 和 $[i+2^{j-1}, 2^j-1]$ 这两个区间中的较小值，递推关系如下：

$$
f[j][i] = \min\{f[j-1][i], f[j-1][i+2^{j-1}]\}
$$

显然，初始条件为：$f[0][i] = a[i]$．预处理时间复杂度 $O(n\log n)$．

```cpp
for (int i = 0; i <= n; i++) {
	st[0][i] = a[i];
}
for (int j = 1; j <= 17; j++) {
	for (int i = 1; i + (1 << j) - 1 <= n; i++) {
		st[j][i] = min(st[j - 1][i], st[j - 1][i + (1 << (j - 1))]);
	}
}
```

在查询 $[l,r]$ 的最小值时，我们可以将长度 $s$ 取 $\log_2$．设结果为 $k$，则 $2^k$ 必定大于等于 $s$ 的一半，且小于 $s$．此时，答案为从 $l$ 开始，长度为 $2^k$ 的区间，和末尾为 $r$，长度为 $2^k$ 的区间的最小值．这两个区间的交集必然是 $[l,r]$．可以列出表达式：

$$
ans=\min\{f[k][l],f[k][r-2^k+1]\}
$$

查询复杂度是 $O(1)$ 的．

```cpp
int query(int l, int r) {
    int len = log2(r - l + 1);
    return min(st[len][l], st[len][r - (1 << len) + 1]);
}
```

> [!warning] `<<` 的优先级问题
> 在 C++ 中， `<<` 和 `>>` 的优先级是低于 `+` 和 `-` 的，因此在进行左移和右移运算时，一定要注意括号问题．
